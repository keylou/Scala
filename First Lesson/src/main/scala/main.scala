object main extends App // extends подключает к объекту метод main
{
  val message = "Hello World!" // val выбирает тип переменной автоматически, если его не указывать
  val x: Int = 2
  println(message)
  println(x)
}
